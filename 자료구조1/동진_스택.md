풀이시간 30분

![스크린샷 2022-07-31 오전 3 32 48](https://user-images.githubusercontent.com/82895809/181936915-1a0c8caa-dd2a-4e7f-a8b2-c62e24edffce.png)


```java
import java.io.BufferedReader;
import java.io.IOException;
import java.io.InputStreamReader;
import java.util.Stack;
import java.util.StringTokenizer;

public class Main {
    Stack<Integer> stack = new Stack<Integer>();
    private static final BufferedReader br = new BufferedReader(new InputStreamReader(System.in));

    public static void main(String[] args) throws IOException {
        Main solution = new Main();
        final int cnt = Integer.parseInt(br.readLine());
        for (int i = 0; i < cnt; i++) {
            solution.method();
        }
    }

    public void method() throws IOException {
        StringTokenizer st = new StringTokenizer(br.readLine());
        final String str = st.nextToken();
        int answer = 0;
        if (str.contains("push")) {
            final int num = Integer.parseInt(st.nextToken());
            stack.push(num);
        } else {
            if (str.contains("top")) {
                if (stack.empty()) {
                    answer = -1;
                } else {
                    answer = stack.peek();
                }
            } else if (str.contains("size")) {
                answer = stack.size();
            } else if (str.contains("pop")) {
                if (stack.empty()) {
                    answer = -1;
                } else {
                    answer = stack.pop();
                }
            } else if (str.contains("empty")) {
                if (stack.empty()) {
                    answer = 1;
                } else {
                    answer = 0;
                }
            }
            System.out.println(answer);
        }
    }
}
```
